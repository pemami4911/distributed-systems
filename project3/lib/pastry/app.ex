defmodule Pastry.App do
    @moduledoc """
    Application API
    """
    def deliver(msg, key) do
        
    end
    
    def forward(msg, key, next_Id) do
        
    end

    def new_leafs(leaf_set) do
        
    end
end